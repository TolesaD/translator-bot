[phases.setup]
cmds = [
    "apt-get update",
    "apt-get install -y ffmpeg"
]

[phases.install]
cmds = [
    "python3 -m ensurepip",
    "python3 -m pip install --upgrade pip",
    "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "python start.py"