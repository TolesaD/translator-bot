[build]
builder = "nixpacks"

[deploy]
startCommand = "python main.py"

# These are just documentation - they don't automatically set variables
[variables]
BOT_TOKEN = "required"
ANNOUNCEMENT_CHANNEL = "optional"